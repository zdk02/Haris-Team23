# Pinned to the exact versions the working Docker image resolved (plan Step 13).
# Direct dependencies only — pip resolves the transitive ones consistently on the same
# platform. To refresh after any dependency change, rebuild and run:
#   docker run --rm haris pip freeze
langgraph==1.2.10
presidio-analyzer==2.2.364
presidio-anonymizer==2.2.364
detect-secrets==1.5.0
# Module 7 (secrets_pii) needs a spaCy model for Presidio; the Dockerfile runs:
#   python -m spacy download en_core_web_sm   (resolves en_core_web_sm 3.8.x for spaCy 3.8)
# spaCy itself is pulled in by presidio-analyzer, so it isn't listed separately.
networkx==3.6.1
pydantic==2.13.4
streamlit==1.61.1
streamlit-agraph==0.0.45
pandas==3.0.5
pytest==9.1.1
faker==40.36.0