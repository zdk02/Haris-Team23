# Direct dependencies, at the versions the working Docker image resolved (plan Step 13).
#
# This file documents INTENT — what Haris actually depends on, and why. The build does not
# read it: the image installs `requirements.lock.txt`, which pins every direct and
# transitive package, so the artefact is reproducible rather than merely "the same versions
# of the things we remembered to list".
#
# To refresh the lockfile after any dependency change:
#   docker build -t haris . && docker run --rm haris pip freeze > requirements.lock.txt

langgraph==1.2.10
presidio-analyzer==2.2.364
presidio-anonymizer==2.2.364
detect-secrets==1.5.0
# Module 7 (secrets_pii) needs a spaCy model for Presidio: en_core_web_sm 3.8.0.
# It is NOT fetched with `python -m spacy download` — that resolves over the network at
# build time and cannot be pinned. requirements.lock.txt installs the model wheel directly
# from its release URL with a #sha256= fragment, so the exact bytes are fixed.
# spaCy itself is pulled in by presidio-analyzer, so it isn't listed separately.
networkx==3.6.1
pydantic==2.13.4
streamlit==1.61.1
streamlit-agraph==0.0.45
pandas==3.0.5
pytest==9.1.1
faker==40.36.0

# boto3 is deliberately NOT pinned here. SESChannel imports it lazily and no-ops without it,
# and the deployed task role grants no SES permission, so the image must not carry it.
# To exercise the SES channel locally:  pip install boto3